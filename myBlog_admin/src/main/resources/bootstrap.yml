spring:
  application:
    name: admin-api
  cloud:
      config:
        discovery:
          enabled: true
          service-id: myBlog-config
        profile: dev

module:
  logging:
    path: E:/workspace/blogLog

