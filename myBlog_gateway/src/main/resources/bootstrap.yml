spring:
  application:
    name: gateway-server
  cloud:
      config:
        discovery:
          service-id: myBlog-config
          enabled: true
        profile: dev
eureka:
  client:
    serviceUrl:
      register-with-eureka: true
      fetch-registry: true
      defaultZone: http://localhost:8761/eureka/



