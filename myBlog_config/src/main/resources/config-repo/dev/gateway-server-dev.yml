spring:
  mvc:
      async:
        request-timeout: 20s
  profiles:
      include: common-dev
zuul:
  routes:
    admin-api:
      path: /admin-api/**
      serviceId: admin-api
    file-api:
      path: /file-api/**
      serviceId: file-server
  host:
    connection-request-timeout-millis: 20000
